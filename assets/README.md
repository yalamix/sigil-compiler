# Test Assets

Place reference assets here for integration tests. Not shipped with the package.

Meshes:
  icosphere.obj     generated by trimesh, ground truth: x^2+y^2+z^2=r^2
  bunny_low.obj     Stanford Bunny, 2503 vertices
  bunny_high.obj    Stanford Bunny, ~70k faces

Textures:
  checker.png        simple checker pattern
  gradient.png       smooth colour gradient

Sounds:
  sine_440hz.wav     440Hz sine, 1s -- should compress to near-zero residual
  footstep.wav       transient + resonance tail -- hybrid representation

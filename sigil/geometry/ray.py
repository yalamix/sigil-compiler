# Ray-surface intersection for validation rendering.
#
# polynomial_ray_intersect(alphas, feature_names, ray_origin, ray_dir) -> t or None
# newton_ray_intersect(f_callable, grad_callable, ray_origin, ray_dir) -> t or None

# Equation utilities shared across SR backends.
#
# polynomial_features(points, degree) -> (feature_matrix, feature_names)
# sympy_to_alphas(sympy_expr, feature_names) -> np.ndarray
# alphas_to_sympy(alphas, feature_names) -> sympy_expr
# evaluate_equation(alphas, feature_names, points) -> np.ndarray

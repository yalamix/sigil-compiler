# Evolutionary SR backend via PySR.
# Slower than sparse_regression; searches operator structure rather than
# optimising within a fixed polynomial basis.
# Warm start: uses PySR warm_start parameter and equation_file seeding.

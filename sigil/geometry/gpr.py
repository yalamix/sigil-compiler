# GPR fitting and querying.
# Adapted from prototype.py.
#
# fit_gpr(X, y) -> (gpr, scaler)
# query_gpr(gpr, scaler, n_query) -> (query_points, gpr_values)
# fit_overlap_gpr(mesh, overlap_vertices, epsilon) -> (gpr, scaler)

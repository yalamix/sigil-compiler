# Patch segmentation strategies.
#
# farthest_point_sampling(mesh, n_seeds) -> list[vertex_index]
# extract_patch_radius(mesh, seed, radius) -> patch_vertices
# extract_patch_voronoi(mesh, seeds) -> list[patch_vertices]
# compute_geodesic_distance(mesh, source_vertex) -> distances
